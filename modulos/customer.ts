const paginationPerPage = 10; 

interface Customer {
    name: string;
    age: number;
}

export { paginationPerPage, Customer }

//o export pode ser exportado assim ou podemos simplesmente exportar direto na declaração da interface e da constante.